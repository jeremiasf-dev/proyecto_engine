# Este es el inicio de mi propio motor gráfico en 2D.
# Herramientas a utilizar:
# > C#/.net 8.0
# > SDL2-CS como librería de gráficos y sonidos.
# > Geany IDE
#
# Lo primero que busco hacer es implementar cada concepto matemático que vaya aprendiendo.
# Luego, buscaré integrar esos conceptos de forma gráfica.
# 
# --- Comenzamos ... ---
#
# Instalación de SDL2-CS.
#
# dotnet add package SDL2-CS
# Dependencia en linux: paquetes "libsdl2-2.0-0" y "libsdl2-dev"
# 
# Ciclo de vida de un programa en C#:
# Main: Lugar donde se iniciliza lo necesario y se arranca la ejecución.
# Inicialización de subsistemas (Video, Events y Timer)
# Game loop: Eventos, Actualización, Renderizado
# Finalización (Liberación de recursos)
#
# Agregadas carpetas Engine y Game para separar la lógica de cada uno.
# Agregada carpeta Math. Ahí mismo empezaré a programar.
# Agregado Proportion.cs con el método para hallar la incógnita en una proporcionalidad directa.
# Agregado tipo Vector2D con (double x, double y) como componentes.
# Agregados métodos en Vector2D(Suma, resta, multiplicación, longitud/magnitud, normalización, producto punto. 
# 
# Aprendiendo geometría...
# 
# Agregado: World/Tile.cs, Tilemap.cs, Engine/Graphics/Windows.cs, Input/; solo estructura, resta por hacer
# Decisión: SDL2 descartado. Elegido OpenTK por ser orientado a C#.
# Agregado WindowsEngine.cs (reemplazo de Windows.cs) con ventana lista para inicializarse y recibir input por actualización por frame.
# Ahora toca crear el renderizado, se me ocurre crear clases con colores y funciones para dibujar poligonos según las vértices dadas...
# ... toca seguir aprendiendo geometría, pero antes, me gustaría renderizar un triángulo y tener un pequeño programa que...
# ... me permita utilizar el motor como calculadora matemática y física.
# 	> Utilizar operaciones de Engine.Math.Vector2D para representar vectores en un eje cartesiano 2D.
#	> Lo mismo que arriba, pero con polígonos, geometría, trigonometría, álgebra lineal y cuadrática
#
# Se me está ocurriendo transformar las coordenadas x0,y0 (actualmente situadas en la esquina superior
# izquierda, hacia el centro de la ventana, y utilizarla como referencia para calcular a partir de allí
# Cuadrantes y puntos cardinales se originarán desde ahí, además de métodos para transformar vectores y
# polígonos. Luego se le implementará física y trigonometría sí o sí (en el orden que correspondan).
